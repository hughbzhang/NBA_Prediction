//OT Code doesn't work

if (line.at(line.length()-2)=='O' && line.at(line.length()-1)=='T') {
	    		char posNum = line.at(line.length()-3);
	    		string OT;

	    		if (posNum >= '0' && posNum <='9') {
	    			line = line.substr(0, line.length()-3);
	    			OT = posNum + "OT";
	    		} else {
	    			line = line.substr(0, line.length()-2);
	    			OT = '0' + "OT";
	    		}
	    	}
